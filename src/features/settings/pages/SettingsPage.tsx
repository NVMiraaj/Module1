import { ModulePlaceholder } from '../../../components/ModulePlaceholder';

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="Configure application preferences, permissions, and reference data."
    />
  );
}
